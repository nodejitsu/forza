{
  "name": "estragon",
  "version": "0.0.1",
  "devDependencies": {
    "json-stream": "0.2.x",
    "assert-called": "0.1.x",
    "async": "0.2.x"
  },
  "scripts": {
    "test": "test/run",
    "preinstall": "./configure --with-plugin uptime --with-plugin mem --with-plugin cpu --with-plugin process --with-plugin heartbeat --with-plugin logs --with-plugin start && make"
  },
  "bin": {
    "estragon": "estragon"
  },
  "publishConfig": {
    "registry": "http://reg.njitsu.net:5984"
  }
}
